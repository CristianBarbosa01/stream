import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    margin: 5,
    marginBottom: 20
  },
  image: {
    borderRadius: 5
  },
  title: {
    fontSize: 14,
    color: '#fff',
    marginTop: 10
  }
});